# Порт API-сервера (server/index.ts) и origin(ы) клиента для CORS (через запятую).
# VITE_API_BASE_URL добавится на этапе 04 (Task 10, Docker/nginx) — сейчас не читается.
PORT=4000
CORS_ORIGIN=http://localhost:5173
